# Krishna Trust - Web Application Documentation

## Table of Contents
1. [Project Overview](#project-overview)
2. [Tech Stack](#tech-stack)
3. [Project Structure](#project-structure)
4. [Installation & Setup](#installation--setup)
5. [Database Schema](#database-schema)
6. [Modules & Features](#modules--features)
   - [Public Website](#public-website)
   - [Admin Panel](#admin-panel)
7. [Routes Reference](#routes-reference)
8. [Controllers](#controllers)
9. [Models](#models)
10. [Exports (Excel Reports)](#exports-excel-reports)
11. [Localization](#localization)
12. [Authentication & Authorization](#authentication--authorization)
13. [Security Hardening](#security-hardening)
14. [Performance Optimizations](#performance-optimizations)

---

## Project Overview

**Krishna Trust** is a full-stack web application built for a charitable trust organization. It serves two purposes:

- **Public Website** — Displays trust information, services, photo gallery (impacts), testimonials, contact form, and an online donation form.
- **Admin Panel** — A secured backend for trust administrators to manage beneficiaries (Labharthis), donations, employees, attendance, expenses, content, and generate monthly reports.

---

## Tech Stack

| Layer | Technology |
|---|---|
| Backend Framework | Laravel 9 (PHP ^8.0.2) |
| Database | MySQL |
| Frontend | Blade Templates, Bootstrap CSS, jQuery |
| Maps | Leaflet.js |
| Excel Export | Maatwebsite/Excel v3.1 |
| JS Validation | proengsoft/laravel-jsvalidation v4.9 |
| Translation | stichoza/google-translate-php v5.2 |
| Authentication | Laravel built-in Auth + Sanctum |

---

## Project Structure

```
krishna-trust/
├── app/
│   ├── Exports/               # Excel export classes
│   │   ├── AttendanceExport.php
│   │   ├── ContactExport.php
│   │   ├── DonationExport.php
│   │   ├── EmployeeWithdrawalExport.php
│   │   ├── ExpenseExport.php
│   │   └── LabharthiExport.php
│   ├── Helpers/
│   │   └── helpers.php        # Global helper functions
│   ├── Http/
│   │   └── Controllers/
│   │       ├── AuthController.php          # Login / Logout
│   │       ├── WebController.php           # Public website pages
│   │       └── Admin/                      # Admin panel controllers
│   │           ├── AdminController.php
│   │           ├── AttendanceController.php
│   │           ├── ContactController.php
│   │           ├── ContentController.php
│   │           ├── DonationController.php
│   │           ├── EmployeeController.php
│   │           ├── ExpenseController.php
│   │           ├── LabharthiController.php
│   │           ├── ReportController.php
│   │           ├── ServiceController.php
│   │           └── TestimonialController.php
│   └── Models/
│       ├── Area.php
│       ├── Attendance.php
│       ├── Contact.php
│       ├── Content.php
│       ├── Donation.php
│       ├── Employee.php
│       ├── EmployeeWithdrawal.php
│       ├── Expense.php
│       ├── Labharthi.php
│       ├── Service.php
│       ├── Testimonial.php
│       └── User.php
├── database/
│   └── migrations/            # All database migrations
├── resources/
│   └── views/
│       ├── admin/             # Admin panel Blade views
│       ├── auth/              # Login & register views
│       ├── layouts/           # Layout templates
│       ├── module/            # Shared modules (donation form, policies)
│       └── web/               # Public website Blade views
├── routes/
│   └── web.php                # All web routes
└── public/
    ├── images/                # Static images
    └── js/                    # Custom JS files
```

---

## Installation & Setup

### Requirements
- PHP >= 8.0.2
- Composer
- MySQL
- WAMP / XAMPP / Laravel Valet

### Steps

```bash
# 1. Clone the repository
git clone <repository-url>
cd krishna-trust

# 2. Install PHP dependencies
composer install

# 3. Copy environment file
cp .env.example .env

# 4. Generate application key
php artisan key:generate

# 5. Configure the .env file
```

### Environment Configuration (`.env`)

```env
APP_NAME="Krishna Trust"
APP_ENV=local
APP_KEY=              # generated by artisan key:generate
APP_DEBUG=true
APP_URL=http://localhost

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=krishna_trust
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=file
SESSION_LIFETIME=120
```

```bash
# 6. Run database migrations
php artisan migrate

# 7. Create storage symlink (for uploaded images)
php artisan storage:link

# 8. Serve the application
php artisan serve
```

---

## Database Schema

### `users`
| Column | Type | Notes |
|---|---|---|
| id | bigint | Primary key |
| name | string | Admin name |
| email | string | Unique |
| password | string | Hashed |
| is_admin | boolean | Grants admin access |
| created_at / updated_at | timestamps | |

---

### `labharthi_form` (Beneficiaries)
| Column | Type | Notes |
|---|---|---|
| id | bigint | Primary key |
| labharthi_number | string | Unique, area-prefixed (e.g., `RJ0001`) |
| position | integer | Display order |
| status | tinyint | 1 = active, 0 = inactive |
| area_id | bigint | Foreign key → `area.id` |
| name | string | Beneficiary name |
| address | text | Current address |
| latitude / longitude | decimal | GPS coordinates |
| native_place | string | Place of origin |
| cast / sub_cast | string | Caste information |
| adhar_number | string(12) | Aadhaar card number |
| mobile_number | string | With +91 prefix |
| category | enum | `vidhva`, `vidhur`, `rejected`, `other` |
| work | string | Occupation |
| identification_mark | string | Physical identification |
| income_source | text | Source of income |
| property | text | Property details |
| relative_info | text | Relative information |
| reasion_for_not_working | text | Reason for not working |
| reasion_for_tifin | text | Reason for applying for tiffin |
| comment_from_trust | text | Internal trust notes |
| tifin_starting_date | date | Tiffin service start date |
| tifin_ending_date | date | Tiffin service end date |
| reasion_for_tifin_stop | string | Reason tiffin was stopped |
| deleted_at | timestamp | Soft delete |
| created_at / updated_at | timestamps | |

---

### `donations`
| Column | Type | Notes |
|---|---|---|
| id | bigint | Primary key |
| receipt_number | string | Auto-incremented receipt |
| date | date | Donation date |
| full_name | string | Donor name |
| mobile_number | string | With +91 prefix |
| address | text | Donor address |
| amount | decimal | Donation amount |
| donation_for | string | Purpose of donation |
| comment | text | Optional notes |
| pan_number | string | PAN card number |
| payment_mode | enum | `cash`, `cheque`, `online` |
| bank_name | string | For cheque payments |
| cheque_number | string | For cheque payments |
| cheque_date | date | For cheque payments |
| transaction_id | string | For online payments |
| transaction_date | datetime | For online payments |
| deleted_at | timestamp | Soft delete |
| created_at / updated_at | timestamps | |

---

### `employees`
| Column | Type | Notes |
|---|---|---|
| id | bigint | Primary key |
| name | string | Employee name |
| address | text | Address |
| mobile_number | string | Mobile number |
| adhar_number | string(12) | Aadhaar number |
| email | string | Email address |
| password | string | (Unused) |
| status | string | `active` / `inactive` |
| salary | decimal | Monthly salary |
| image | string | Profile image path |
| deleted_at | timestamp | Soft delete |
| created_at / updated_at | timestamps | |

---

### `employee_withdrawals`
| Column | Type | Notes |
|---|---|---|
| id | bigint | Primary key |
| employee_id | bigint | Foreign key → `employees.id` |
| withdrawal_amount | decimal | Amount withdrawn |
| withdrawal_date | date | Withdrawal date |
| created_at / updated_at | timestamps | |

---

### `attendances`
| Column | Type | Notes |
|---|---|---|
| id | bigint | Primary key |
| labharthi_id | bigint | Foreign key → `labharthi_form.id` |
| attendance | tinyint | `0` = absent, `1` = present, `2` = not specified |
| attendance_date | date | Date of attendance |
| created_at / updated_at | timestamps | |

---

### `contents` (Media Gallery)
| Column | Type | Notes |
|---|---|---|
| id | bigint | Primary key |
| name | string | Optional label |
| image | string | Image path in storage |
| upload_date | date | Date image is attributed to |
| created_at / updated_at | timestamps | |

---

### `services`
| Column | Type | Notes |
|---|---|---|
| id | bigint | Primary key |
| title / description | string/text | Service details |
| status | tinyint | Show/hide on website |
| created_at / updated_at | timestamps | |

---

### `testimonials`
| Column | Type | Notes |
|---|---|---|
| id | bigint | Primary key |
| name | string | Person name |
| post | string | Job title / position |
| message | text | Testimonial text |
| status | tinyint | Show/hide on website |
| created_at / updated_at | timestamps | |

---

### `contacts`
| Column | Type | Notes |
|---|---|---|
| id | bigint | Primary key |
| name | string | Sender name |
| email | string | Sender email |
| mobile | string | Mobile with +91 prefix |
| subject | string | Message subject |
| message | text | Message body |
| created_at / updated_at | timestamps | |

---

### `expenses`
| Column | Type | Notes |
|---|---|---|
| id | bigint | Primary key |
| amount | decimal | Expense amount |
| purpose | string | Purpose of expense |
| comment | text | Optional notes |
| date | date | Expense date |
| deleted_at | timestamp | Soft delete |
| created_at / updated_at | timestamps | |

---

### `area`
| Column | Type | Notes |
|---|---|---|
| id | bigint | Primary key |
| name | string | Area full name |
| slug | string | URL-friendly name |
| short_form | string | Used as prefix for labharthi numbers |
| pincode | string | PIN code |
| status | tinyint | Active/inactive |
| created_at / updated_at | timestamps | |

---

## Modules & Features

### Public Website

Accessible without login at the root URL.

| Page | URL | Description |
|---|---|---|
| Home | `/` | Landing page with services, testimonials, donation form, latest content |
| About | `/about` | About the trust, beneficiary count |
| Contact | `/contact` | Contact form |
| Impact | `/impact` | Photo gallery grouped by month |
| Privacy Policy | `/privacy-policy` | Locale-aware (English/Gujarati) |
| Terms & Conditions | `/terms-and-conditions` | Locale-aware (English/Gujarati) |

**Donation Form (Home Page)**
- Fields: Full Name, Mobile, Address, Amount, Donation For, PAN Number, Transaction ID, Transaction Date
- Receipt number is auto-generated and pre-filled
- Mobile number is formatted with `+91` prefix before saving

**Contact Form**
- Fields: Name, Email, Mobile, Subject, Message
- Validates 10-digit mobile number, formats with `+91` prefix

---

### Admin Panel

Accessible at `/admin/*` — requires login and `admin` role.

#### Dashboard (`/admin/dashboard`)
Displays summary cards:
- Total Contents (media uploads)
- Total Donations
- Total Labharthis (beneficiaries)
- Total Donation Amount

---

#### Labharthi Management (`/admin/labharthi`)

Manages beneficiaries receiving the trust's tiffin/food service.

| Action | URL | Description |
|---|---|---|
| List | `GET /admin/labharthi` | Paginated list with search |
| Create | `GET /admin/labharthi/create` | Create form |
| Store | `POST /admin/labharthi` | Save new labharthi |
| Edit | `GET /admin/labharthi/{id}/edit` | Edit form |
| Update | `PUT /admin/labharthi/{id}` | Update record |
| Delete | `DELETE /labharthi` | Soft delete |
| Export | `GET /labharthi/export` | Download Excel of all labharthis |
| Position | `GET /admin/labharthi/position` | Drag-and-drop reorder |
| Update Order | `POST /admin/labharthi/update-order` | Save new order |
| Next Number | `GET /admin/get-next-labharthi-number/{areaId}` | AJAX: get next auto-number |

**Labharthi Number Format:** `{AREA_SHORT_FORM}{4-digit-sequence}` — e.g., `RJ0001`

**Category types:** `vidhva` (widow), `vidhur` (widower), `rejected`, `other`

---

#### Donation Management (`/admin/donation`)

| Action | URL | Description |
|---|---|---|
| List | `GET /admin/donation` | Paginated list with search |
| Create | `GET /admin/donation/create` | Create form |
| Store | `POST /admin/donation` | Save new donation |
| Edit | `GET /admin/donation/{id}/edit` | Edit form |
| Update | `PUT /admin/donation/{id}` | Update record |
| Delete | `DELETE /donation` | Soft delete |
| Export | `GET /donation/export` | Download Excel |

**Payment Modes:** `cash`, `cheque`, `online`

---

#### Employee Management (`/admin/employee`)

| Action | URL | Description |
|---|---|---|
| List | `GET /admin/employee` | Paginated list |
| Create | `GET /admin/employee/create` | Create form |
| Store | `POST /admin/employee` | Save new employee |
| Edit | `GET /admin/employee/{id}/edit` | Edit form |
| Update | `PUT /admin/employee/{id}` | Update record |
| Delete | `DELETE /employee` | Soft delete |
| Withdrawal | `GET /employee/withdrawal/{id}` | View employee salary withdrawals |
| Store Withdrawal | `POST /employee/withdrawal` | Save a withdrawal entry |

**Withdrawal Feature:** Tracks partial salary withdrawals per month. Shows final salary = salary − total withdrawals for that month.

---

#### Attendance Tracking (`/admin/attendance`)

Records daily attendance for active Labharthis.

| Action | URL | Description |
|---|---|---|
| List | `GET /admin/attendance` | List all active labharthis |
| Store | `POST /admin/attendance` | Mark / update attendance via AJAX |

**Attendance Values:** `0` = Absent, `1` = Present, `2` = Not Specified

---

#### Content / Media Gallery (`/admin/contents`)

Manages photos shown on the public "Impact" page.

| Action | URL | Description |
|---|---|---|
| List | `GET /admin/contents` | Paginated list |
| Create | `GET /admin/contents/create` | Upload form |
| Store | `POST /admin/contents` | Upload image |
| Edit | `GET /admin/contents/{id}/edit` | Edit upload date |
| Update | `PUT /admin/contents/{id}` | Update image/date |
| Delete | `DELETE /contents` | Delete image + file |

Images are stored in `storage/app/public/content_images/`.

---

#### Services (`/admin/service`)

Manages the service cards shown on the homepage.

| Action | URL | Description |
|---|---|---|
| List | `GET /admin/service` | List all services |
| Create / Store | `GET/POST /admin/service/create` | Add service |
| Edit / Update | `GET/PUT /admin/service/{id}/edit` | Edit service |
| Delete | `DELETE /service` | Soft delete |

---

#### Testimonials (`/admin/testimonial`)

Manages testimonial cards shown on the homepage.

| Action | URL | Description |
|---|---|---|
| List | `GET /admin/testimonial` | List all testimonials |
| Create / Store | `GET/POST /admin/testimonial/create` | Add testimonial |
| Edit / Update | `GET/PUT /admin/testimonial/{id}/edit` | Edit testimonial |
| Delete | `DELETE /testimonial` | Soft delete |

---

#### Expense Tracking (`/admin/expense`)

Tracks the trust's operational expenses.

| Action | URL | Description |
|---|---|---|
| List | `GET /admin/expense` | List all expenses |
| Create / Store | `GET/POST /admin/expense/create` | Add expense |
| Edit / Update | `GET/PUT /admin/expense/{id}/edit` | Edit expense |
| Delete | `DELETE /expense` | Soft delete |

---

#### Contact Inquiries (`/admin/contact`)

View-only listing of messages submitted via the public Contact form.

---

#### Monthly Reports (`/admin/monthly-report`)

Download Excel reports filtered by month/year for each module.

| Report | URL | Export File Name |
|---|---|---|
| Donation | `GET /admin/monthly-report/donation?month_year=YYYY-MM` | `{Month-Year}-Donation-List.xlsx` |
| Labharthi | `GET /admin/monthly-report/labharthi?month_year=YYYY-MM` | `{Month-Year}-Labharthi-List.xlsx` |
| Attendance | `GET /admin/monthly-report/attendance?month_year=YYYY-MM` | `{Month-Year}-Attendance-List.xlsx` |
| Expense | `GET /admin/monthly-report/expense?month_year=YYYY-MM` | `{Month-Year}-Expense-List.xlsx` |
| Contact | `GET /admin/monthly-report/contact?month_year=YYYY-MM` | `{Month-Year}-Contact-List.xlsx` |
| Employee Withdrawal | `GET /admin/monthly-report/employee-withdrawal?month_year=YYYY-MM` | `{Month-Year}-Employee-Withdrawal-List.xlsx` |

---

#### Leaflet Map (`/admin/leaflet-map`)

Interactive map showing the GPS location of all Labharthis who have latitude/longitude recorded.

---

#### Change Password (`/admin/changePassword`)

Allows admin to change their own password. Verifies current password before updating.

---

## Routes Reference

### Public Routes

```
GET  /                           → WebController@index
GET  /about                      → WebController@about
GET  /contact                    → WebController@contact
POST /contact                    → WebController@contactPost
GET  /impact                     → WebController@impacts
POST /donation                   → WebController@donationStore
GET  /privacy-policy             → WebController@privacyPolicy
GET  /terms-and-conditions       → WebController@termsAndConditions
```

### Auth Routes
```
GET  /login                      → AuthController@showLogin
POST /login                      → AuthController@login
POST /logout                     → AuthController@logout
```

### Admin Routes (prefix: `/admin`, middleware: `auth`, `admin`)
```
GET  /admin/dashboard
GET  /admin/lang/{locale}        → Session language switcher (en/gu)
GET  /admin/leaflet-map

Resource: /admin/contents        → ContentController
Resource: /admin/labharthi       → LabharthiController
Resource: /admin/attendance      → AttendanceController
Resource: /admin/donation        → DonationController
Resource: /admin/expense         → ExpenseController
Resource: /admin/employee        → EmployeeController
Resource: /admin/service         → ServiceController
Resource: /admin/testimonial     → TestimonialController

GET  /admin/contact              → ContactController@index
GET  /admin/monthly-report       → ReportController@index
GET  /admin/monthly-report/donation
GET  /admin/monthly-report/labharthi
GET  /admin/monthly-report/attendance
GET  /admin/monthly-report/expense
GET  /admin/monthly-report/contact
GET  /admin/monthly-report/employee-withdrawal

GET  /admin/changePassword
POST /admin/changePassword
GET  /admin/labharthi/position
POST /admin/labharthi/update-order
GET  /admin/get-next-labharthi-number/{areaId}
```

---

## Controllers

| Controller | Namespace | Responsibility |
|---|---|---|
| `WebController` | `App\Http\Controllers` | Public website pages |
| `AuthController` | `App\Http\Controllers` | Login, logout, register |
| `AdminController` | `App\Http\Controllers\Admin` | Dashboard, map, change password |
| `LabharthiController` | `App\Http\Controllers\Admin` | Beneficiary CRUD + export + ordering |
| `DonationController` | `App\Http\Controllers\Admin` | Donation CRUD + export |
| `EmployeeController` | `App\Http\Controllers\Admin` | Employee CRUD + salary withdrawals |
| `AttendanceController` | `App\Http\Controllers\Admin` | Attendance marking (AJAX) |
| `ContentController` | `App\Http\Controllers\Admin` | Media gallery CRUD |
| `ServiceController` | `App\Http\Controllers\Admin` | Services CRUD |
| `TestimonialController` | `App\Http\Controllers\Admin` | Testimonials CRUD |
| `ExpenseController` | `App\Http\Controllers\Admin` | Expense CRUD |
| `ContactController` | `App\Http\Controllers\Admin` | View contact submissions |
| `ReportController` | `App\Http\Controllers\Admin` | Monthly Excel report downloads |

---

## Models

| Model | Table | Soft Delete | Relationships |
|---|---|---|---|
| `User` | `users` | No | — |
| `Labharthi` | `labharthi_form` | Yes | `belongsTo Area` |
| `Area` | `area` | No | — |
| `Donation` | `donations` | Yes | — |
| `Employee` | `employees` | Yes | — |
| `EmployeeWithdrawal` | `employee_withdrawals` | No | `belongsTo Employee` |
| `Attendance` | `attendances` | No | `belongsTo Labharthi` |
| `Content` | `contents` | No | — |
| `Service` | `services` | No | — |
| `Testimonial` | `testimonials` | No | — |
| `Contact` | `contacts` | No | — |
| `Expense` | `expenses` | Yes | — |

---

## Exports (Excel Reports)

All exports use **Maatwebsite/Excel** and are organized in `app/Exports/`.

| Export Class | Filters | Output |
|---|---|---|
| `DonationExport` | By month-year | Donation list |
| `LabharthiExport` | All / by month | Labharthi list |
| `AttendanceExport` | By month-year | Attendance list |
| `ContactExport` | By month-year | Contact submissions |
| `EmployeeWithdrawalExport` | By month-year | Withdrawal records |
| `ExpenseExport` | By month-year | Expense records |

---

## Localization

The application supports **English** and **Gujarati** languages.

- Language is toggled via `GET /admin/lang/{locale}` (`en` or `gu`)
- Selected locale is stored in the PHP session
- Views dynamically serve locale-specific content (e.g., `en-privacy-policy`, `gu-privacy-policy`)
- Validation and portal messages use Laravel's `__()` translation helper

Translation files are located in `resources/lang/`.

---

## Authentication & Authorization

### Login
- URL: `GET /login`
- Only users with `is_admin = true` can access the admin panel
- After login, non-admin users are redirected back to login with an error

### Middleware
- `auth` — Ensures the user is logged in
- `admin` — Ensures the logged-in user has `is_admin = true`

### Admin Access Check
The `User` model has an `isAdmin()` method used during login to verify admin privilege.

### Password Management
Admins can change their password from `/admin/changePassword`. The current password is verified using `Hash::check()` before updating.

---

## Helper Functions

Located in `app/Helpers/helpers.php` and auto-loaded globally.

| Function | Description |
|---|---|
| `convertYmdToMdy($date)` | Convert `Y-m-d` to `m-d-Y` format |
| `plainAmount($formattedAmount)` | Strip non-numeric chars from amount string |
| `formattedAmount($amount)` | Format number to 2 decimal places |
| `formatByGroups($number, $group)` | Format a number string into spaced groups |
| `encryptResponse(...)` | AES-256-CBC encryption |
| `decryptAesResponse(...)` | AES-256-CBC decryption |

-------------------------------------------------------------------------

## Security Hardening

The following security measures have been implemented across the application.

### 1. Route Protection (Critical Fix)

All DELETE, export, and employee withdrawal routes are placed **inside** the `auth + admin` middleware group. Previously these were publicly accessible without authentication.

**Protected routes (now require login + admin role):**
- `DELETE /admin/labharthi` — delete beneficiary
- `DELETE /admin/donation` — delete donation
- `DELETE /admin/contents` — delete content/image
- `DELETE /admin/service` — delete service
- `DELETE /admin/testimonial` — delete testimonial
- `DELETE /admin/expense` — delete expense
- `DELETE /admin/employee` — delete employee
- `GET /admin/donation/export` — download donation Excel
- `GET /admin/labharthi/export` — download labharthi Excel
- `GET /admin/employee/withdrawal/{id}` — view withdrawals
- `POST /admin/employee/withdrawal` — save withdrawal

**File:** `routes/web.php`

---

### 2. Login Brute-Force Protection

The `POST /login` route has rate limiting applied:

```php
Route::post('/login', [...])→middleware('throttle:5,1');
// Max 5 login attempts per minute per IP
// Returns HTTP 429 Too Many Requests on violation
```

**File:** `routes/web.php`

---

### 3. HTTP Security Headers

Security headers added via Apache `.htaccess` using `mod_headers`:

| Header | Value | Purpose |
|---|---|---|
| `X-Frame-Options` | `SAMEORIGIN` | Prevents clickjacking attacks |
| `X-Content-Type-Options` | `nosniff` | Prevents MIME-type sniffing |
| `X-XSS-Protection` | `1; mode=block` | Enables legacy browser XSS filter |
| `Referrer-Policy` | `strict-origin-when-cross-origin` | Controls referrer info sent |
| `Permissions-Policy` | `geolocation=(), camera=(), microphone=()` | Restricts browser API access |
| `X-Powered-By` | *(removed)* | Hides PHP version from responses |
| `Server` | *(removed)* | Hides server software info |

> **Note:** Requires `mod_headers` enabled in Apache/WAMP.

**File:** `public/.htaccess`

---

### 4. Session Hardening

Two session security settings strengthened in `config/session.php`:

| Setting | Before | After | Reason |
|---|---|---|---|
| `encrypt` | `false` | `true` | Session data encrypted at rest in storage |
| `same_site` | `lax` | `strict` | Stricter CSRF cross-site protection |

Other session security already in place:
- `http_only: true` — cookies inaccessible to JavaScript
- Session files stored server-side (not in browser cookies)

**File:** `config/session.php`

---

### 5. CORS Restriction

CORS configuration restricted from wildcard to application domain only:

| Setting | Before | After |
|---|---|---|
| `allowed_origins` | `['*']` (any domain) | `[env('APP_URL')]` (your domain only) |
| `allowed_methods` | `['*']` (all methods) | `['GET', 'POST']` |

CORS only applies to `api/*` and `sanctum/csrf-cookie` paths.

**File:** `config/cors.php`

---

### 6. Input Validation & Sanitization (Existing)

All controller methods validate and sanitize inputs:
- Format validation: Aadhaar (12-digit numeric), PAN (`/^[A-Z]{5}[0-9]{4}[A-Z]{1}$/`), mobile (10-digit)
- Type validation: enum (category, payment_mode), numeric (amount, latitude/longitude)
- Unique constraints: Aadhaar number, labharthi number, receipt number
- `strip_tags()` applied to all string inputs before saving
- Mobile numbers stored with `+91` prefix after format validation

---

### 7. CSRF Protection (Existing)

- `VerifyCsrfToken` middleware active on all web routes
- CSRF token included in all layout `<head>` sections via `<meta name="csrf-token">`
- No routes excluded from CSRF verification

---

## Performance Optimizations

### 1. Database Indexes

Migration `2026_03_04_000001_add_performance_indexes.php` adds indexes on all frequently searched and filtered columns:

| Table | Column | Index Name | Benefit |
|---|---|---|---|
| `labharthi_form` | `status` | `idx_labharthi_status` | Every list query filters by status |
| `labharthi_form` | `name` | `idx_labharthi_name` | Search by name (LIKE) |
| `labharthi_form` | `mobile_number` | `idx_labharthi_mobile` | Search by mobile |
| `labharthi_form` | `area_id` | `idx_labharthi_area` | FK join with area table |
| `donations` | `date` | `idx_donations_date` | Monthly report filtering |
| `donations` | `full_name` | `idx_donations_full_name` | Search by donor name |
| `attendances` | `attendance_date` | `idx_attendance_date` | Daily attendance lookup |
| `attendances` | `labharthi_id` | `idx_attendance_labharthi` | FK join performance |
| `contents` | `upload_date` | `idx_contents_upload_date` | Gallery sorting |
| `employees` | `status` | `idx_employees_status` | Employee list filtering |

> The migration also fixes legacy `0000-00-00` invalid date values in the `donations` table by setting them to `NULL`.

**File:** `database/migrations/2026_03_04_000001_add_performance_indexes.php`

---

### 2. Eager Loading (N+1 Query Prevention)

`LabharthiController` now eager-loads the `area` relationship to eliminate N+1 database queries:

### 3. Homepage Caching

Services and testimonials are cached for **10 minutes** on the public homepage since they change infrequently:

Cache is automatically **invalidated** whenever an admin creates, updates, or deletes a service or testimonial:


### 4. Image Lazy Loading

All below-the-fold images on public pages use the browser-native `loading="lazy"` attribute. This defers image loading until the image scrolls into the viewport, reducing initial page load time and bandwidth.

| View | Images with lazy loading |
|---|---|
| `web/index.blade.php` | Service images, gallery images, testimonial photos |
| `web/impacts.blade.php` | All monthly gallery images |



---

*This documentation was generated based on the full codebase analysis of the Krishna Trust Laravel application.*
